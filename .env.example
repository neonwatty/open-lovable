# Local Development Configuration for Claude Code-only Operation

# Local sandbox directory for file operations
LOCAL_SANDBOX_PATH=./sandbox

# Vite development server port
VITE_PORT=5173

# Optional - Development settings
NODE_ENV=development

# Note: This version operates locally without external API dependencies
# E2B, Firecrawl, and AI provider API keys are no longer required